variables:
- name: version
  ${{ if eq(variables.isPrerelease, 'true') }}:
    value: '$(semanticVersion).$(Build.BuildId)-SNAPSHOT'
  ${{ else }}:
    value: '$(semanticVersion).$(Build.BuildId)'
- name: targetDir
  value: 'target'
- name: gpgKeyName
  value: 'brad.nicholes@oneidentity.com'
